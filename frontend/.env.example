# Optional: full API origin. When set, the dev app uses CORS to this host instead of the Vite /api proxy.
# Example: VITE_API_BASE_URL=http://127.0.0.1:8080
# Leave unset to use relative /api (reverse proxy in production, Vite proxy in dev if you remove .env.development)
